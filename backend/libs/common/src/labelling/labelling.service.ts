import { Injectable } from '@nestjs/common';

@Injectable()
export class LabellingService {
  async labelData(data: any): Promise<any> {
    // Labelling service implementation when needed
    return data;
  }
}
